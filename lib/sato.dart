import 'package:flutter/material.dart';

class SatoRoute extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("佐藤君とのトークルーム"),
      ),
      body: Container(
        height: double.infinity,
        color: Colors.lime,
      ),
    );
  }
}